{
  "name": "skeet-inventory",
  "private": true,
  "workspaces": ["apps/api", "apps/web", "apps/worker"],
  "scripts": {
    "build": "npm --workspace skeet-inventory-worker run build && npm --workspace @skeet-inventory/api run build && npm --workspace skeet-inventory-web run build",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm --workspace @skeet-inventory/api run typecheck && npm --workspace skeet-inventory-web exec tsc -- --noEmit && npm --workspace skeet-inventory-worker exec tsc -- --noEmit",
    "db:generate": "npm --workspace @skeet-inventory/api run db:generate",
    "db:migrate": "npm --workspace @skeet-inventory/api run db:migrate"
  }
}
